{
  "name": "retail-performance-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Development website for the Retail Performance Dashboard using mock data only.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "preview": "vite preview --host 127.0.0.1",
    "check": "node --check js/app.js && node --check js/data/core-data.js && node --check tests/core-data.test.js",
    "test:data": "node tests/core-data.test.js"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
